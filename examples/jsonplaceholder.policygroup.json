{
  "version": "1.0",
  "localToken": "my-proxy-token",
  "policies": [
    {
      "baseUrl": "https://jsonplaceholder.typicode.com",
      "upstreamToken": "token_123",
      "rules": [
        {
          "route": "/users",
          "method": "GET",
          "action": "allow",
          "response_filter": {
            "type": "include_fields",
            "fields": [
              "[].id",
              "[].name",
              "[].address.city",
              "[].address.geo.*"
            ]
          }
        },
        {
          "route": "/users/*",
          "method": "GET",
          "action": "allow",
          "response_filter": {
            "type": "include_fields",
            "fields": ["id", "name", "address.zipcode"]
          }
        }
      ]
    },
    {
      "baseUrl": "https://api.browserbase.com",
      "rules": [
        {
          "route": "/v1/sessions/*",
          "method": "GET",
          "action": "allow",
          "response_filter": {
            "type": "include_fields",
            "fields": ["id", "projectId", "proxyBytes"]
          }
        }
      ]
    }
  ]
}
